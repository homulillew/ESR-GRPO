# AREX-Turbo 在 BrowseComp-Plus 上的实验结果与 Bad Case 分析

> 分析日期：2026-08-14  
> 数据范围：BrowseComp-Plus validation，共 830 道题  
> 推理模型：AREX-Turbo 4B  
> 运行协议：AREX native XML adapted to BC-Plus fixed corpus  
> 最大交互轮数：100

## 摘要

本实验使用 AREX-Turbo 在 BrowseComp-Plus 固定语料环境中完成 830 道题的推理。原始评测文件报告完成率 93.13%、Accuracy 71.89%、Evidence Recall 81.32%。复核完整轨迹和评测程序后，Accuracy 71.89% 不能作为严格端到端正确率：57 条没有正常提交答案的轨迹仍被送入 Judge，其中 14 条空回答被 Judge 直接判为正确；另有 1 条已提交轨迹发生 Judge 解析错误，被排除在 Accuracy 分母之外。

按“只有正常调用 `finish` 并被 Judge 判为正确才算成功”的口径，严格端到端正确率为 70.12%。正常提交后的条件正确率为 75.29%。

本次分析得到四项主要结论：

1. **检索覆盖决定了大部分基础成功率，但不能解释全部错误。** 190 条正常提交但回答错误的轨迹中，86 条标准证据召回率低于 0.5，另有 47 条已经召回全部标准证据却仍然答错。
2. **错误轨迹的问题集中在搜索结果选择和证据使用。** 错误轨迹平均检索 507.4 个不同文档，却只实际访问 2.90 个不同文档；正确轨迹平均检索 202.7 个文档，实际访问 3.33 个文档。
3. **`update_context` 会保存错误判断，也经常丢失来源。** 3848 次状态更新中有 53.20% 没有 URL。部分轨迹在早期找到正确候选后，因为错误解释线索而把正确候选写成“已排除”，后续搜索持续受错误状态影响。
4. **轮数耗尽和协议错误构成独立失败来源。** 45 条轨迹因工具协议错误结束，12 条达到 100 轮上限。严格失败的 248 条轨迹中，有 183 条进入过第 91 轮后的强制结束阶段。

这些结果验证了 Evidence-State Routing for Group Relative Policy Optimization（ESR-GRPO，利用证据与任务状态分配动作信用的组相对策略优化）所针对的问题确实存在，但尚不能证明 ESR-GRPO 本身有效。AREX 轨迹没有 ESR-GRPO 的 Evidence ID、TaskState 版本、验证动作和组内优势，因而只能用于诊断问题和设计实验，不能直接验证动作级信用分配。

---

## 1. 实验对象与分析范围

### 1.1 结果目录

本报告使用以下文件：

| 文件或目录 | 内容 |
|---|---|
| [`evaluation_qwen32b.json`](evaluation_qwen32b.json) | 830 道题的 Judge 结果和汇总指标 |
| [`trajectories/`](trajectories/) | 每一步模型输入、推理、工具调用、工具返回和状态更新 |
| [`runs/`](runs/) | 提交给评测器的标准化运行结果 |
| [`failed/`](failed/) | 57 条未正常完成轨迹的失败摘要 |
| [`judge_cache/Qwen3-32B/`](judge_cache/Qwen3-32B/) | Qwen3-32B 的逐题评测结果 |
| [`judge_cache/Lanz-Auto/`](judge_cache/Lanz-Auto/) | 另一套 Judge 的逐题评测结果，用于一致性检查 |

`run_summary.json` 只记录最后一次重试批次，其中 `selected=102`、`completed=94`、`failed=8`，不能用作 830 道题的全量汇总。本报告以 830 个 `runs`、830 个 `trajectories` 和 `evaluation_qwen32b.json` 为准。

### 1.2 当前结果代表什么

本次运行保留 AREX-Turbo 的 XML 工具协议和 `update_context` 状态压缩方式，并把搜索环境改为 BrowseComp-Plus 固定语料。模型可以执行：

- `search`：一次生成一组查询，从固定语料检索候选文档；
- `visit`：打开已经由搜索返回的页面；
- `update_context`：使用一段自然语言状态替换此前的交互历史；
- `finish`：提交答案、证据和置信度。

该运行没有复现 AREX 论文中的完整外层验证与重新研究循环，也禁用了不属于固定语料环境的搜索工具。因此，本报告将结果称为“AREX-Turbo 在 BrowseComp-Plus 上的适配基线”，不把它等同于论文完整系统的最终能力。

### 1.3 指标定义

本报告使用以下定义：

- **正常提交**：轨迹状态为 `completed`，模型成功调用合法的 `finish`；
- **严格正确**：正常提交并被 Judge 判为正确；
- **条件正确率**：只在正常提交的轨迹中计算正确率；
- **Evidence Recall**：搜索返回的文档 ID 与标准相关文档 ID 的召回率；
- **实际访问**：模型通过 `visit` 成功读取文档正文；
- **全部引用已访问**：最终答案中的每一个引用 URL 都曾经由 `visit` 成功打开；
- **强制结束阶段**：Harness 从第 91 轮开始提示模型尽快完成并调用 `finish`。

Evidence Recall 只表示标准文档进入过搜索结果，不表示模型打开、理解或采用了该文档。因此，Evidence Recall 与实际证据使用需要分开分析。

---

## 2. 评测口径复核

### 2.1 轨迹完成情况

```text
830 条轨迹
├─ 773 条正常提交
│  ├─ 582 条 Judge 判定正确
│  ├─ 190 条 Judge 判定错误
│  └─ 1 条 Judge 解析失败
└─ 57 条未正常提交
   ├─ 45 条协议错误
   └─ 12 条达到 100 轮上限
```

对应比例为：

| 状态 | 数量 | 占全部题目的比例 |
|---|---:|---:|
| 正常提交且正确 | 582 | 70.12% |
| 正常提交但错误 | 190 | 22.89% |
| 正常提交但 Judge 解析失败 | 1 | 0.12% |
| 协议错误 | 45 | 5.42% |
| 达到最大轮数 | 12 | 1.45% |

### 2.2 原始 Accuracy 为什么虚高

当前评测程序会读取所有 `runs/*.json`，即使轨迹状态不是 `completed`，也会把空响应连同标准答案发送给 Judge。评测提示还要求 Judge 在没有置信度时填 100。

在 57 条未正常提交轨迹中：

- 14 条被 Qwen3-32B 判为正确；
- 这些轨迹均没有 `output_text`；
- Judge 提取出的答案与提示中的标准答案完全相同；
- 这 14 条不能算作模型成功完成任务。

原始 Accuracy 实际计算为：

$$
\frac{596}{829}=71.89\%.
$$

其中 596 包含 582 条真实成功和 14 条未提交轨迹上的 Judge 假阳性；829 则排除了 1 条 Judge 解析失败。

严格端到端正确率应为：

$$
\mathrm{Accuracy}_{\mathrm{strict}}
=
\frac{582}{830}
=
70.12\%.
$$

正常提交后的条件正确率为：

$$
\mathrm{Accuracy}_{\mathrm{completed}}
=
\frac{582}{773}
=
75.29\%.
$$

建议后续同时报告这两个指标。严格正确率反映完整系统表现，条件正确率用于区分答案质量和协议可靠性。

### 2.3 Judge 一致性

Qwen3-32B 和另一套 Judge 在 687 条共同覆盖且正常提交的轨迹上得到：

| 指标 | 数值 |
|---|---:|
| 两个 Judge 的一致率 | 98.11% |
| Cohen's kappa | 0.950 |
| 分歧题目 | 13 |
| Qwen3-32B 正确率 | 74.38% |
| 另一套 Judge 正确率 | 75.40% |

整体一致性较高，但 13 条分歧题足以影响约 1.5 个百分点。这些题集中在：

- 正确答案后附带真实的全名或解释；
- 人名存在转写和拼写变体；
- 书名介词或一个字母不同；
- 模型给出了标准答案，同时额外给出可能不属于答案集合的信息。

这 13 条应全部人工复核，不能直接用于训练方法判断。

### 2.4 置信度与校准

当前汇总中的 Calibration Error 受到未提交轨迹的影响，因为这些轨迹没有模型置信度，却被 Judge 填为 100。只在 772 条正常提交且 Judge 可解析的轨迹上按相同分箱方法重算，校准误差约为 13.78%。

正常提交轨迹的置信度分布如下：

| 置信度区间 | 轨迹数 | 正确数 | 实际正确率 | 平均置信度 |
|---|---:|---:|---:|---:|
| 0–49 | 110 | 4 | 3.64% | 30.97% |
| 50–69 | 52 | 18 | 34.62% | 60.35% |
| 70–79 | 17 | 8 | 47.06% | 73.24% |
| 80–89 | 31 | 26 | 83.87% | 84.00% |
| 90–94 | 36 | 31 | 86.11% | 91.33% |
| 95–99 | 426 | 403 | 94.60% | 96.71% |
| 100 | 100 | 92 | 92.00% | 100.00% |

大部分低置信度轨迹确实错误，但仍有 36 条错误轨迹给出至少 90% 的置信度，其中 22 条已经召回全部标准证据。这些样例主要反映证据解释、关系绑定或最终答案检查失败。

---

## 3. 全量实验结果

### 3.1 正确、错误和未完成轨迹的行为差异

| 指标 | 正确提交 582 条 | 错误提交 190 条 | 未完成 57 条 |
|---|---:|---:|---:|
| 平均轮数 | 29.78 | 82.18 | 60.49 |
| 中位轮数 | 16 | 92 | 69 |
| 平均 `search` 动作 | 20.32 | 64.05 | 46.89 |
| 平均 `visit` 动作 | 5.49 | 7.60 | 4.72 |
| 平均 `update_context` 动作 | 2.88 | 9.32 | 6.77 |
| 平均标准证据召回率 | 93.0% | 54.1% | 53.7% |
| 平均不同检索文档数 | 202.65 | 507.40 | 429.77 |
| 平均不同访问文档数 | 3.33 | 2.90 | 2.32 |
| 平均最终证据条数 | 4.85 | 2.84 | 0 |

错误轨迹平均搜索动作约为正确轨迹的 3.15 倍，检索候选文档约为 2.50 倍，但实际访问的不同页面更少。按均值粗略计算：

- 正确轨迹打开的不同页面约占检索候选的 1.64%；
- 错误轨迹打开的不同页面约占检索候选的 0.57%。

错误轨迹还表现出更高的页面访问错误率。以每条轨迹内部的访问错误比例计算，正确轨迹平均为 11.47%，错误轨迹平均为 25.83%。

因此，主要差异不是错误轨迹缺少搜索次数，而是它们没有从大量搜索结果中选择并检查最关键的页面。

### 3.2 轮数与正确率

| 完成轮数 | Judge 可解析的正常提交数 | 正确数 | 正确率 | 平均 Evidence Recall |
|---|---:|---:|---:|---:|
| 1–20 | 347 | 328 | 94.52% | 92.39% |
| 21–40 | 106 | 101 | 95.28% | 93.05% |
| 41–60 | 51 | 47 | 92.16% | 92.03% |
| 61–80 | 43 | 39 | 90.70% | 93.27% |
| 81–99 | 215 | 67 | 31.16% | 63.14% |
| 100 | 10 | 0 | 0% | 21.03% |

前 80 轮内完成的轨迹正确率均超过 90%。81 轮以后，正确率下降到 31.16%；刚好在第 100 轮提交的 10 条轨迹全部错误。

长轨迹既包含更难的问题，也反映当前搜索过程缺少有效收敛条件。该相关性不能直接解释为“轮数越多导致错误”，但可以确定：模型在失败前通常已经进行了大量无效扩展。

### 3.3 Evidence Recall 与正确率

在 772 条正常提交且 Judge 可解析的轨迹中：

| Evidence Recall | 轨迹数 | 正确数 | 正确率 |
|---|---:|---:|---:|
| 0 | 21 | 0 | 0% |
| 大于 0、小于 0.5 | 75 | 10 | 13.33% |
| 0.5 至小于 1 | 217 | 160 | 73.73% |
| 1 | 459 | 412 | 89.76% |

190 条 Judge 判定错误的正常提交轨迹中：

- 21 条没有召回任何标准证据；
- 65 条召回率大于 0、小于 0.5；
- 57 条召回率在 0.5 至 1 之间；
- 47 条召回全部标准证据。

检索覆盖不足可以解释 86 条错误。其余 104 条错误已经召回至少一半标准证据，需要继续区分页面选择、证据解释、状态维护和答案提交问题。

### 3.4 搜索不是简单重复，而是无法收敛

每次 `search` 可以包含多个子查询。按子查询统计：

| 指标 | 正确提交 | 错误提交 | 未完成 |
|---|---:|---:|---:|
| 平均子查询数 | 57.90 | 190.00 | 139.32 |
| 中位子查询数 | 28 | 218 | 164 |
| 平均精确重复比例 | 1.06% | 2.38% | 1.98% |

精确重复比例很低，说明模型通常会改写查询，而不是逐字重复。错误表现为连续生成大量相近但没有缩小问题范围的搜索方向。后续分析应关注查询是否由具体待查问题驱动，以及新结果是否改变当前答案或待查问题，而不能只统计重复字符串。

---

## 4. `update_context` 与证据可追溯性

### 4.1 状态更新内容

830 条轨迹共执行 3848 次 `update_context`。基于状态文本中的显式标记统计：

| 内容 | 出现次数 | 占状态更新比例 |
|---|---:|---:|
| 至少包含一个 URL | 1801 | 46.80% |
| 没有任何 URL | 2047 | 53.20% |
| 包含 source、URL、docid 或 evidence 标记 | 1537 | 39.94% |
| 包含待解决问题相关表述 | 2293 | 59.59% |
| 包含失败或排除方向 | 2006 | 52.13% |
| 包含下一步计划 | 2638 | 68.56% |

这些类别会重叠。统计结果说明当前状态通常同时承担四项任务：保存事实、保存来源、记录失败方向和制定下一步计划。自然语言状态没有强制把这些对象分开，也没有要求每条结论绑定原始证据。

### 4.2 最终引用来自哪里

正常提交轨迹一共输出 3355 条最终引用。根据 URL 在轨迹中的来源分类：

| 引用来源 | 全部引用 | 正确轨迹 | 错误或解析失败轨迹 |
|---|---:|---:|---:|
| 对应页面实际打开过 | 2360（70.34%） | 2071（73.34%） | 289（54.43%） |
| URL 只在搜索结果中出现 | 795（23.70%） | 631（22.34%） | 164（30.89%） |
| URL 在整条轨迹中从未出现 | 200（5.96%） | 122（4.32%） | 78（14.69%） |

错误轨迹中，45.57% 的最终引用没有对应的页面正文；14.69% 的引用 URL 甚至没有出现在搜索结果中。

按整条轨迹检查“所有最终引用是否都访问过”：

| 分组 | 全部引用均已访问 | 比例 |
|---|---:|---:|
| 正确提交 | 223 / 582 | 38.32% |
| 错误提交 | 43 / 190 | 22.63% |
| 所有 830 条轨迹 | 266 / 830 | 32.05% |

在正常提交且 Judge 可解析的轨迹中：

- 全部引用均已访问时，正确率为 83.83%；
- 至少一个引用未访问时，正确率为 70.95%。

该差异是相关关系，不能单独证明打开全部引用会导致正确率提升。困难问题可能同时产生更多未访问引用和更多错误。不过，它说明引用与页面访问的一致性可以作为重要过程指标。

### 4.3 最后状态是否保留最终证据

对于至少执行过一次 `update_context` 的轨迹，检查最后一版状态是否仍包含最终引用 URL：

| 分组 | 有状态轨迹数 | 最后状态中的最终引用覆盖率均值 | 中位数 |
|---|---:|---:|---:|
| 正确提交 | 372 | 73.58% | 100% |
| 错误或解析失败 | 157 | 39.01% | 0% |

错误轨迹最后状态中的最终引用覆盖率中位数为 0，说明它们在回答前使用的当前状态往往不能指向最终声称依赖的证据。这个现象既可能来自状态压缩丢失来源，也可能来自模型在 `finish` 中临时添加未验证引用。

---

## 5. 协议错误和停止行为

### 5.1 45 条协议失败

| 最终错误 | 数量 |
|---|---:|
| 模型输出中没有合法 AREX `<tool_call>` | 29 |
| `update_context` 缺少 `context` | 3 |
| `search.query` 不是非空字符串数组 | 3 |
| `visit` 缺少 `goal` | 2 |
| `finish` 缺少 `confidence` | 2 |
| `visit.url` JSON 无法解析 | 3 |
| `finish.evidences` 字段不符合要求 | 1 |
| `search` 缺少 `query` | 1 |
| `search.query` JSON 无法解析 | 1 |

运行过程中还记录到：

- 115 次无工具调用格式错误；
- 54 次 `finish` 格式错误；
- 43 次 `search` 格式错误。

这些数字是错误事件数，不是互斥的轨迹数。部分格式错误经过 Harness 重试后恢复，45 条轨迹最终仍然失败。

当前汇总中的 `Average Tool Calls` 会把 `None` 动作和格式错误后的工具尝试一起计入。全量工具计数包含 115 个名为 `None` 的记录和 54 次额外的无效 `finish` 尝试，因此该指标不完全等同于环境成功执行的动作数。

### 5.2 强制结束阶段

共有 237 条轨迹进入第 91 轮后的强制结束阶段：

| 结果 | 数量 |
|---|---:|
| 最终正确 | 54 |
| 提交错误或 Judge 解析失败 | 158 |
| 仍未完成 | 25 |

严格失败的 248 条轨迹中，183 条进入过强制结束阶段，占 73.79%。在 237 条进入该阶段的轨迹中，严格成功率只有 22.78%。

强制结束提示本身不是已经证明的失败原因，因为只有搜索很久的困难轨迹才会触发该提示。它反映的是此前搜索过程没有形成可提交的答案：模型无法根据当前状态确定哪个问题仍未解决，也无法判断继续搜索的预期收益。

---

## 6. Bad Case 分类方法

### 6.1 分类原则

一条失败轨迹可能同时存在多个问题。例如，标准证据已经进入搜索结果，但模型没有打开；随后状态又保存了错误候选；最后还因 `finish` 格式错误而失败。为了避免重复计数，人工标注应记录**第一个导致正确答案无法继续形成的环节**，并把后续问题保存为次要标签。

建议按下面的顺序检查：

```text
评测标签是否可靠
→ 动作协议是否合法
→ 标准证据是否进入搜索结果
→ 模型是否打开关键页面
→ 模型是否正确解释证据
→ 当前状态是否保留正确答案、证据关系和待查问题
→ 验证是否发现冲突或缺口
→ 模型是否在预算内提交正确答案
```

### 6.2 主分类

| 编号 | 分类 | 判定条件 | 主要观察字段 |
|---|---|---|---|
| E0 | 评测或标注问题 | 两个 Judge 分歧、标准答案与固定语料冲突、正确附加信息被判错 | Judge raw、标准答案、最终答案 |
| P1 | 协议失败 | 工具参数、XML 或 JSON 不合法，轨迹没有正常提交 | `status`、`error`、失败动作 |
| R1 | 检索覆盖不足 | 关键标准文档没有进入搜索结果，Evidence Recall 较低 | `retrieved_docids`、Recall |
| S1 | 页面选择失败 | 关键文档或正确答案已经出现在搜索结果，但模型没有打开 | `search` 结果、`visit` URL |
| I1 | 证据解释失败 | 模型打开了相关材料，但误解限定条件、时间、角色或否定关系 | 页面原文、reasoning、finding |
| B1 | 关系绑定失败 | 事实分别正确，但答案、实体、论文、作者或年份之间对应错误 | 多份证据与最终答案 |
| M1 | 状态维护失败 | 正确信息在早期出现，后续状态遗漏、覆盖或错误排除 | `context_updates` 版本变化 |
| V1 | 验证失败 | 当前答案与证据冲突，但模型仍标为已确认或高置信度提交 | 最后状态、验证清单、置信度 |
| T1 | 停止或提交失败 | 长时间搜索后无法形成答案、被迫猜测、达到轮数上限 | 轮数、强制提示、`finish` |

其中，E0 和 P1 应先于方法错误处理。只有评测可靠且轨迹协议合法时，R1 至 T1 才能用于分析搜索策略和训练方法。

### 6.3 推荐标注字段

每条人工分析记录至少保存：

```text
query_id
question
gold_answer
run_status
judge_correct
judge_disagreement
turns
evidence_recall
retrieved_doc_count
visited_doc_count
first_failure_stage
secondary_failure_tags
critical_evidence_url
critical_evidence_first_seen_turn
critical_evidence_visited_turn
correct_candidate_first_mentioned_turn
state_corruption_turn
forced_finish
final_answer
confidence
analysis_note
related_esr_hypothesis
```

`first_failure_stage` 只填一个主分类，`secondary_failure_tags` 可以填写多个后续问题。

---

## 7. 代表性 Bad Case

### 7.1 Query 543：答案在搜索结果中，但没有打开并采用

轨迹：[`trajectories/543.json`](trajectories/543.json)

| 项目 | 内容 |
|---|---|
| 标准答案 | Arsenal Tula |
| 模型答案 | Manchester City |
| 轮数 | 82 |
| Evidence Recall | 1.0 |
| 置信度 | 100% |
| 主分类 | S1 页面选择失败 |
| 次要分类 | B1、M1、V1 |

`Arsenal Tula` 从第 8 轮开始多次出现在搜索结果片段中。相关片段明确说明 Artem Dzyuba 曾被租借到 Arsenal Tula。模型没有打开该材料，反而选择 Denis Cheryshev 作为目标球员。

最后一版 `update_context` 写入了未经原始证据支持的说法：Cheryshev 被 Manchester City 租借到 Spartak Moscow。状态将这条错误关系标为已确认。模型最终以 100% 置信度提交 Manchester City。

该轨迹包含三步连续错误：

1. 搜索已经返回正确候选，但页面选择错误；
2. 当前状态保存了错误的球员—租借球队关系；
3. 提交前的检查只核对了状态内部是否自洽，没有回到原始材料验证关系。

### 7.2 Query 1126：正确候选在第 4 轮出现，却因线索语义反转被排除

轨迹：[`trajectories/1126.json`](trajectories/1126.json)

| 项目 | 内容 |
|---|---|
| 标准答案 | Kwon So-hee |
| 模型答案 | 无法确定 |
| 轮数 | 93 |
| Evidence Recall | 1.0 |
| 置信度 | 15% |
| 主分类 | I1 证据解释失败 |
| 次要分类 | M1、T1 |

第 4 轮搜索结果已经给出：

```text
Sogumm
birth_name: Kwon So-hee
birth_date: 8 March 1994
```

后续结果还表明她参加过 audition program，并在 2019 年发布首张完整专辑。问题中的线索是“采访描述她晚餐吃了一些属于 haram 的食物”。模型把它理解为“该艺人应当是穆斯林并避免 haram 食物”，因此认为 Sogumm 不符合要求。

从第 62 轮开始，`update_context` 持续把 Kwon So-hee 保存为已排除候选。即使后续搜索再次返回她的真实姓名和专辑信息，状态中的错误解释仍然主导决策。最终模型在第 93 轮声明无法确定。

这条轨迹说明状态本身不能保证正确性。状态如果保存了错误解释，后续上下文重建会放大该错误。需要同时保存原始证据、当前解释和具体待查问题，并在提交前对照原文检查解释。

### 7.3 Query 15：找到目标论文，却回答了另一篇论文的期刊

轨迹：[`trajectories/15.json`](trajectories/15.json)

| 项目 | 内容 |
|---|---|
| 标准答案 | Romanian Statistical Review |
| 模型答案 | Romanian Journal of Economics |
| 轮数 | 6 |
| Evidence Recall | 1.0 |
| 置信度 | 95% |
| 主分类 | B1 关系绑定失败 |
| 次要分类 | V1 |

模型正确找到使用约 20% 人口样本、包含约 170 万就业者并使用多项逻辑回归的目标论文。该论文发表于 `Romanian Statistical Review`。模型还找到同一作者同年发表的另一篇论文，后者发表于 `Romanian Journal of Economics`。

问题要求目标论文的期刊。模型却把“同年另一篇论文”的期刊作为答案。两份论文、两个期刊和“共享关键词”线索分别被识别，但它们之间的对应关系绑定错误。

### 7.4 Query 245：没有区分 lead author 和共同作者

轨迹：[`trajectories/245.json`](trajectories/245.json)

| 项目 | 内容 |
|---|---|
| 标准答案 | Jacek Rojowski |
| 模型答案 | Bożena Muszyńska |
| 轮数 | 10 |
| Evidence Recall | 1.0 |
| 置信度 | 97% |
| 主分类 | B1 关系绑定失败 |
| 次要分类 | V1 |

模型找到的论文确实包含蘑菇、元素提取、微波消解、酚酸和重金属等全部线索，但它根据作者在相关研究中的出现频率选择了 Bożena Muszyńska，没有按照问题要求确认目标论文的第一作者。

该错误不能通过继续增加相似论文解决。关键是把“论文—实验描述—作者顺序—问题要求”明确对应起来。

### 7.5 Query 1022：答案和证据都已找到，但 `finish` 协议失败

轨迹：[`trajectories/1022.json`](trajectories/1022.json)

| 项目 | 内容 |
|---|---|
| 标准答案 | 2300; Sara Beatriz Maldonado |
| 模型最后状态 | 已保存 2300 years 和 Sara Beatriz Maldonado |
| 轮数 | 47 |
| Evidence Recall | 1.0 |
| 最终状态 | failed |
| 主分类 | P1 协议失败 |

最后一版状态已经保存科学家的全名、2016 年论文和“放射性碳测年约 2300 年”的原文。模型第一次调用 `finish` 时证据结构不合法；第二次尝试又把字段写成 `docid`，而协议要求每个元素只能包含 `evidence` 和 `url`。轨迹最终没有产生可评测答案。

这类轨迹不应被归为检索失败或推理失败。它说明当前动作协议仍需要单独的合法率指标，并可能需要协议 SFT 冷启动。

### 7.6 Query 666：正确候选反复出现，但错误任务假设控制了搜索

轨迹：[`trajectories/666.json`](trajectories/666.json)

| 项目 | 内容 |
|---|---|
| 标准答案 | Michael Caine |
| 模型答案 | 无法确认身份 |
| 轮数 | 91 |
| Evidence Recall | 0.25 |
| Judge 结果 | 解析失败 |
| 主分类 | I1 证据解释失败 |
| 次要分类 | M1、T1 |

Michael Caine 从第 45 轮开始多次出现在搜索结果中，模型还打开了包含他与 Christopher Nolan 合作关系的页面。但是，模型把“截至 2023 年正在写第二本小说”错误地解释为“此人的主要职业必须是小说家”，并在最后状态中明确排除演员候选。

这条错误假设使正确候选即使反复出现也无法进入最终答案。最后状态保存了错误的任务分解，而没有保存“Michael Caine 是否满足其余条件”这一具体待查问题。

### 7.7 Query 291：Judge 拒绝了正确答案后的真实补充信息

轨迹：[`trajectories/291.json`](trajectories/291.json)

| 项目 | 内容 |
|---|---|
| 标准答案 | Baha |
| 模型答案 | Baha (Tyler Kamau Mbaya) |
| Evidence Recall | 1.0 |
| Qwen3-32B | 错误 |
| 另一套 Judge | 正确 |
| 主分类 | E0 评测问题 |

Qwen3-32B 的解释承认附加的全名是正确内容，却仍以“包含不必要信息”为由判错。这与评测提示中“允许正确的附加细节”的规则不一致。

类似的分歧还包括：

- `Glafkos/Glafcos Clerides`；
- `Irfan/Irrfan Ali Khan`；
- `Sunset in/at Biafra`；
- `Claude Beri/Berel Langmann`。

这些样例必须在分析模型方法前先完成人工裁决。

### 7.8 Query 942：答案包含标准成员，但额外加入两人

轨迹：[`trajectories/942.json`](trajectories/942.json)

| 项目 | 内容 |
|---|---|
| 标准答案 | Turbett, Sweet, Carriero, Wolfe |
| 模型答案 | Turbett, Sweet, Carriero, Wolfe, Greig, Bewick |
| 轮数 | 40 |
| Evidence Recall | 1.0 |
| 置信度 | 100% |
| 主分类 | B1 关系绑定或集合边界失败 |

模型回答包含四个标准成员，但又加入 Greig 和 Bewick。该题需要检查两个游戏的 credits、角色范围以及“development team”在标准答案中的筛选口径。它可能是模型过度扩展答案，也可能存在 Benchmark 标注边界问题。因此，这条轨迹同时需要证据关系复核和人工 Judge 复核。

---

## 8. Bad Case 的全量统计信号

### 8.1 正确答案原文是否进入轨迹

对 190 条 Judge 判错轨迹和 1 条 Judge 解析失败轨迹，用标准答案字符串匹配搜索结果、页面正文、模型推理和状态。结果如下：

| 标准答案原文出现位置 | 数量 |
|---|---:|
| 搜索结果中出现，但没有在打开页面中出现 | 50 |
| 已打开页面中出现 | 45 |
| 只在模型推理或状态中出现 | 1 |
| 没有检测到标准答案原文 | 95 |

这是保守下界。拼写变体、缩写、别名和数值表达差异会被归入“没有检测到”。例如 `Irfan/Irrfan`、`Sunset in/at Biafra` 即使语义接近，也无法通过完整字符串匹配。

该统计至少说明：191 条已提交失败中，有 95 条在轨迹中直接暴露过标准答案原文；其中 45 条甚至打开过包含标准答案的页面。检索成功以后仍然存在明显的页面选择和证据使用损失。

### 8.2 明确表达不确定性的错误答案

191 条已提交错误或解析失败轨迹中，有 38 条最终答案明确包含以下表达：

- 无法确定；
- 无法确认；
- 没有找到符合全部条件的候选；
- 只能提供最佳猜测。

正确轨迹中只有 1 条出现同类表达。该指标可以区分“模型形成了错误但高置信度的答案”和“搜索未收敛后被迫提交”。两类问题对应不同的分析重点：前者检查证据解释和验证，后者检查待查问题与停止条件。

### 8.3 高置信度错误

190 条错误提交中有 36 条置信度至少为 90%，其中 22 条 Evidence Recall 为 1。这些轨迹优先用于检查：

- 当前状态是否把错误候选标为已确认；
- 最终答案是否绑定了错误实体、时间或作者角色；
- 验证清单是否只检查状态内部一致性；
- 最终引用是否真的支持答案中的关键关系。

---

## 9. 对 ESR-GRPO 实验假设的影响

### 9.1 假设一：保存原始 Evidence 是否必要

AREX 轨迹提供了三类支持证据：

1. 53.20% 的 `update_context` 没有 URL，状态更新后无法稳定恢复原始来源；
2. 错误轨迹中只有 54.43% 的最终引用对应实际打开的页面；
3. Query 1126 等轨迹把错误解释写入状态后，没有原文恢复和重新检查机制。

这些结果支持“原始证据与当前判断分开保存”的设计动机。它们不能证明模型会正确使用 `read_evidence(evidence_id)`。该问题需要通过 ESR-GRPO rollout 和消融实验验证：

- 原始 Evidence + `read_evidence`；
- 只保存摘要；
- 不提供原文恢复；
- 用初始查询或搜索摘要代替原文。

比较答案正确率、访问次数、原文恢复次数和错误 `finding` 修正率。

### 9.2 假设二：TaskState 字段能否表达证据关系

AREX 的主要状态问题不是缺少更多自然语言，而是对象之间的关系没有固定位置：

- Query 15 混淆目标论文与同年另一篇论文的期刊；
- Query 245 混淆第一作者和共同作者；
- Query 543 混淆球员、比赛和租借球队；
- Query 1126 把正确候选保存为已排除；
- Query 666 保存了错误的任务类型假设。

ESR-GRPO 的 TaskState 需要在实验中检查以下能力：

- `answer` 是否始终是一份当前可提交的完整答案；
- `supporting_evidence` 是否只包含当前答案真正采用的证据；
- 每条 `finding` 是否准确说明证据支持哪个关系及其边界；
- `gaps` 是否保存具体、可继续查询的问题；
- 多跳、并列证据、冲突证据和排除候选能否由当前字段表达；
- 验证后产生的具体缺口能否推动后续搜索。

AREX 轨迹可以作为字段覆盖测试集，但必须在 ESR-GRPO 协议下重新 rollout，才能判断当前字段是否足够。

### 9.3 假设三：动作级信用分配是否准确

正确轨迹平均仍包含 20.32 次搜索、5.49 次访问和2.88次状态更新。54 条正确轨迹进入了第91轮后的强制结束阶段。普通全轨迹 GRPO 会让这些轨迹中的大量无关搜索和错误中间判断共享正优势。

这些结果支持缩小正信用范围的动机，但当前 AREX 日志不能直接生成可靠的 ESR-GRPO 动作掩码，原因包括：

- 没有稳定的 Evidence ID 与动作对应关系；
- `update_context` 中的来源不完整；
- 没有 TaskState 版本差分；
- 没有独立的 `verify_answer` 结果；
- 每道题只有一条基线轨迹，没有 GRPO 组内优势。

因此，动作级信用分配准确率必须在 ESR-GRPO rollout 上评估。AREX 成功轨迹只能帮助构造“哪些无效动作不应因最终成功而被强化”的先验案例。

---

## 10. 下一轮实验计划

### 10.1 先修正评测流程

在修改模型方法前，先完成以下工作：

1. 只有 `run_status=completed` 的轨迹才能进入答案 Judge；
2. 未完成轨迹直接记为严格错误，不让 Judge 从标准答案生成答案；
3. Judge 解析失败按严格错误报告，同时进入人工复核列表；
4. 置信度只读取模型 `finish.confidence`，不为未提交轨迹填 100；
5. 同时报告严格正确率和正常提交后的条件正确率；
6. 人工复核两个 Judge 分歧的 13 条正常提交轨迹；
7. 单独报告协议合法率，不把协议失败混入答案推理错误；
8. 区分工具尝试数、合法动作数和环境成功执行数。

修正后的当前基线应至少报告：

```text
严格正确率：70.12%
正常提交条件正确率：75.29%
完成率：93.13%
协议失败率：5.42%
最大轮数失败率：1.45%
平均 Evidence Recall：81.32%
正常提交轨迹的校准误差：约 13.78%
```

### 10.2 建立人工 Bad Case 标注集

建议分两部分：

**确定性检查：**

- 对全部 57 条未完成轨迹自动标注协议错误或最大轮数；
- 对全部 13 条 Judge 分歧轨迹进行人工裁决；
- 对 54 次 `finish` 格式错误和 43 次 `search` 格式错误检查是否经过恢复。

**分层抽取 50 条正常提交失败轨迹：**

- 15 条 Evidence Recall 低于 0.5；
- 10 条标准答案在搜索结果中但没有打开；
- 10 条打开过包含标准答案的页面；
- 10 条进入强制结束阶段并明确表达无法确定；
- 5 条置信度至少 90% 且 Evidence Recall 为 1。

不同抽样层可能重叠。最终按“第一个失败环节”形成互斥主分类，并保存次要标签。

### 10.3 验证 Evidence 与 TaskState

在 ESR-GRPO 的首批 rollout 中重点记录：

- Evidence 创建、去重和不可修改是否正确；
- 新增或修改的 `finding` 是否建立在当前可见原文上；
- `read_evidence` 是否在冲突检查和错误修正前被合理调用；
- 原文退出上下文后能否通过 Evidence ID 正确恢复；
- TaskState 是否遗漏已经读取的新 Evidence；
- 错误 `finding` 是否会在验证后被修正；
- `gaps` 是否能够引出后来进入最终答案的 Evidence；
- 最终答案、主要证据和验证对象是否保持一致。

### 10.4 验证动作级信用分配

从 30–50 条 ESR-GRPO 成功轨迹中人工标注对最终答案有实质贡献的完整动作，并与系统掩码比较：

- 搜索动作命中率；
- 页面打开动作命中率；
- `read_evidence` 命中率；
- `update_state` 命中率；
- 有效 `needs_revision` 验证命中率；
- 最终答案更新和提交动作命中率；
- 正信用动作整体 Recall；
- 正信用动作误报率；
- 每条成功轨迹获得正信用的动作和策略 token 比例。

对照组应包括：

- 全轨迹 GRPO；
- 只追踪最终主要 Evidence 的动作掩码；
- 完整 ESR-GRPO 掩码；
- 与 ESR-GRPO 稀疏度相同的随机动作掩码。

随机掩码用于判断收益来自正确的状态关系，还是只来自减少参与更新的 token。

---

## 11. 结论边界

本报告中的数字分为三类：

1. **直接统计：** 轨迹状态、轮数、工具调用、Evidence Recall、状态更新、URL 和 Judge 结果；
2. **字符串代理指标：** 标准答案是否在搜索结果、页面正文或状态中直接出现；
3. **人工因果解释：** 代表轨迹中哪个环节最先导致错误。

字符串匹配不能识别别名、拼写变体和语义等价表达，因此“正确答案已经出现”的 95 条是下界。它也不能证明某个页面就是解决问题所必需的唯一证据。

强制结束与错误高度相关，但不能据此认定强制提示导致错误。更困难的题目本来就更容易进入第 91 轮。

当前本地 `runner.py` 与生成轨迹时的代码版本不完全一致：轨迹中包含 `forced_finish_turns`、`finish_format_error`、`no_tool_error` 等字段，错误堆栈还引用了当前文件范围之外的行号，而本地版本没有这些运行逻辑。正式复现实验前应保存生成该批结果时的精确代码 commit、提示词、检索服务版本和启动参数。

最后，AREX 轨迹只能说明 ESR-GRPO 所关注的问题存在。Evidence、TaskState 和动作级信用分配是否改善训练，必须通过相同初始模型、相同数据、相同检索环境和相同最终奖励下的受控对照实验确定。

---

## 12. 当前结论

本次 830 条 AREX-Turbo 轨迹的严格端到端正确率为 70.12%。错误来源可以概括为：

```text
协议与评测问题
├─ 未完成轨迹被 Judge 错判为正确
├─ 工具调用格式失败
└─ 部分答案存在 Judge 或标准答案边界争议

搜索问题
├─ 标准证据没有召回
├─ 搜索结果包含关键页面但没有打开
└─ 大量查询扩展没有缩小待查问题

推理与状态问题
├─ 错误解释限定条件
├─ 混淆实体、论文、作者角色和时间关系
├─ 把正确候选错误标为已排除
├─ 状态没有稳定保留来源
└─ 提交前没有对照原文检查关键关系

停止问题
├─ 长时间搜索后仍没有可提交答案
├─ 第 91 轮后被迫猜测或声明无法确定
└─ 达到最大轮数仍未完成
```

因此，下一步实验不应只扩大搜索次数或继续提高检索候选数量。优先顺序应当是：修正评测口径，建立按第一个失败环节分类的人工标注集，再分别验证 ESR-GRPO 的原始 Evidence、结构化 TaskState 和动作级信用分配。

---

## 13. 可直接执行的复核清单

### 13.1 第一批优先轨迹

下面的题目覆盖当前发现的主要问题，适合作为第一批人工复核对象。一个题目可能同时出现在多个观察维度中，最终主分类仍以第一个失败环节为准。

| 观察目标 | 优先 Query ID | 复核重点 |
|---|---|---|
| 协议失败但已经找到答案 | 1022 | 最后状态、两次 `finish` 参数、可恢复性 |
| 无合法工具调用 | 103、1141 | 强制结束提示、无工具输出、重试过程 |
| 达到最大轮数 | 596、635、734、1004 | 第 91–100 轮是否已有候选，为什么仍未合法提交 |
| 搜索结果含标准答案但没有打开 | 543、268、794、706、1028 | 关键结果排名、页面选择理由、后续状态是否提及 |
| 正确候选被错误排除 | 1126、666 | 线索解释、排除依据、错误状态从何时开始保留 |
| 论文或实体关系绑定错误 | 15、245、543 | 目标对象、桥接对象和问题要求是否明确对应 |
| 高置信度且全部标准证据已召回 | 116、245、291、417、519、543、826、942 | 最终答案是否得到原文直接支持，是否存在拼写或集合边界问题 |
| Judge 分歧 | 1037、1214、128、291、298、324、394、406、519、670、703、823、826 | 附加信息、拼写、转写、书名和数值表达是否应接受 |

### 13.2 单条轨迹的复核步骤

每条轨迹按固定顺序分析：

1. 打开 `evaluation_qwen32b.json`，记录标准答案、Judge 结果、置信度和 Evidence Recall；
2. 打开对应的 `trajectories/<query_id>.json`，确认轨迹是否正常提交；
3. 从问题中拆出实体、时间、关系和答案类型约束；
4. 找到标准答案或关键标准文档第一次进入搜索结果的轮次；
5. 检查模型是否打开该页面；
6. 对照页面正文和模型 reasoning，判断证据解释是否正确；
7. 逐版检查 `context_updates`，找出正确候选何时进入、遗漏或被排除；
8. 检查最后状态中的答案、来源、待查问题和验证清单；
9. 检查最终证据 URL 属于“已访问”“仅搜索出现”还是“轨迹中未出现”；
10. 将第一个失败环节写入 `first_failure_stage`，将后续问题写入 `secondary_failure_tags`；
11. 如果两个 Judge 分歧，先完成人工裁决，再决定是否把该题作为模型 bad case；
12. 记录该题可以检验 ESR-GRPO 的哪个实验假设，不直接根据单条轨迹修改方法。

### 13.3 完成标注后的汇总表

完成第一批人工标注后，应按下面的表格汇总。各主分类互斥，比例分母为已经人工确认的 bad case 数量。

| 主分类 | 数量 | 占比 | 平均轮数 | 平均 Recall | 进入强制结束比例 | 高置信度比例 | 代表 Query |
|---|---:|---:|---:|---:|---:|---:|---|
| P1 协议失败 |  |  |  |  |  |  |  |
| R1 检索覆盖不足 |  |  |  |  |  |  |  |
| S1 页面选择失败 |  |  |  |  |  |  |  |
| I1 证据解释失败 |  |  |  |  |  |  |  |
| B1 关系绑定失败 |  |  |  |  |  |  |  |
| M1 状态维护失败 |  |  |  |  |  |  |  |
| V1 验证失败 |  |  |  |  |  |  |  |
| T1 停止或提交失败 |  |  |  |  |  |  |  |
| E0 评测或标注问题 |  |  |  |  |  |  |  |

当一个分类占比较高时，先检查它是否由评测器、协议或固定语料造成。只有能够在不同题目和不同随机种子中重复出现的模型行为，才进入 ESR-GRPO 的方案细节验证。
